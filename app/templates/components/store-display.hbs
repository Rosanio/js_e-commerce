{{log currentUser.client}}
<div class="row">
  <div class="col-md-8">
    <ul class="list-group">
      {{#each owner.sellingHistory as |item|}}
        {{item-tile class="list-group-item" item=item}}
      {{/each}}
    </ul>
  </div>
  <div class="col-md-4">
    {{#if (and (eq currentUser.client owner) currentUser.client.seller)}}
      {{add-item user=owner addItem="addItem"}}
    {{/if}}
  </div>
</div>
